import { createContext,useContext,useEffect,useState } from 'react';import { api } from '../api/client';
const C=createContext(null);export function AdminAuthProvider({children}){const[user,setUser]=useState(null),[loading,setLoading]=useState(true);const refresh=async()=>{try{const d=await api('/admin/auth/me');setUser(d.user)}catch{setUser(null)}finally{setLoading(false)}};useEffect(()=>{refresh()},[]);const login=async payload=>{const d=await api('/admin/auth/login',{method:'POST',body:payload});setUser(d.user);return d.user};const logout=async()=>{try{await api('/admin/auth/logout',{method:'POST'})}finally{setUser(null)}};return <C.Provider value={{user,loading,login,logout,refresh}}>{children}</C.Provider>}export const useAdminAuth=()=>useContext(C);
