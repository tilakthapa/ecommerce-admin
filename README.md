# Admin Dashboard

This folder contains the approved React/Vite admin dashboard connected to the shared backend API.

See the root `README.md` for installation, Super Admin seeding, environment variables, running and deployment instructions.
